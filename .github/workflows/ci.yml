name: CI Pipeline

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [16.x]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    
    - name: Install dependencies
      run: npm install --legacy-peer-deps
    
    - name: Run tests
      run: npm test -- --watchAll=false --passWithNoTests
      env:
        CI: true
    
    - name: Build application
      run: npm run build
      env:
        NODE_OPTIONS: --openssl-legacy-provider
    
    - name: Create artifact archive
      run: |
        cd build
        tar -czf ../shopizer-shop-build.tar.gz .
        cd ..
    
    - name: Upload build artifacts
      uses: actions/upload-artifact@v3
      with:
        name: shopizer-shop-build-${{ github.sha }}
        path: shopizer-shop-build.tar.gz
        retention-days: 30
    
    - name: Upload build folder
      uses: actions/upload-artifact@v3
      with:
        name: build-folder-${{ github.sha }}
        path: build/
        retention-days: 30

  docker-build:
    runs-on: ubuntu-latest
    needs: test-and-build
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    
    - name: Build Docker image
      run: |
        docker build -t shopizer-shop:${{ github.sha }} .
        docker save shopizer-shop:${{ github.sha }} | gzip > shopizer-shop-docker.tar.gz
    
    - name: Upload Docker image
      uses: actions/upload-artifact@v3
      with:
        name: docker-image-${{ github.sha }}
        path: shopizer-shop-docker.tar.gz
        retention-days: 30
